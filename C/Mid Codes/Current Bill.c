#include<stdio.h>

int main()
{
    int u;
    float a,s,t;
    printf("Enter Unit: ");
    scanf("%d",&u);
    if (u=<50)
    a=4.75*u;
    else if (u=<75)
    a=((a-50)*75);
    else if (u=<100)
    a=((a-75)*
    
    return 0;
}